A little demo of embedding and extending Ruby
---------------------------------------------

To build this, do::

  make

You might need to install ruby1.8-dev or edit the version in the Makefile?

To run it once it is built, do::

  ./rb_curses game_loop.rb
